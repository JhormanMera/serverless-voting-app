apiVersion: v1
kind: Service
metadata:
  name: {{ include "process-vote.fullname" . }}
  labels:
    {{- include "process-vote.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "process-vote.selectorLabels" . | nindent 4 }}
