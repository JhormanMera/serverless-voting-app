apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "process-vote.fullname" . }}-test-connection"
  labels:
    {{- include "process-vote.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "process-vote.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
